(()=>{'use strict';const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const title=document.querySelector('.hero-title');if(title&&!reduced){title.innerHTML=title.innerHTML.split(/(\s+|<br>|<em>|<\/em>)/).map((p,i)=>/^\s+$|<\/?em>|<br>$/.test(p)?p:`<span style="animation-delay:${.12+i*.055}s">${p}</span>`).join('')}const els=[...document.querySelectorAll('.reveal')];if(!('IntersectionObserver'in window)||reduced){els.forEach(e=>e.classList.add('in-view'))}else{const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in-view');io.unobserve(e.target)}}),{threshold:.12});els.forEach(e=>io.observe(e))}if(!reduced&&matchMedia('(hover:hover)').matches){document.querySelectorAll('.tilt').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(800px) rotateX(${-y*5}deg) rotateY(${x*5}deg)`});card.addEventListener('pointerleave',()=>card.style.transform='')})}})();
